# Android手机播放器设计决策

9. **项目私有工具链。** Android SDK 放在 `.android-sdk/`、下载物放在 `.tools/` 并加入忽略规则；仓库用 Gradle Wrapper 8.9 复现构建。
10. **不申请普通悬浮窗权限。** 控制面板由用户明确授权的 AccessibilityService 使用 `TYPE_ACCESSIBILITY_OVERLAY`，并保持 `canRetrieveWindowContent=false`。
11. **前台游戏在悬浮面板开始时捕获。** 只使用窗口切换事件的包名，不读取页面内容；外部包变化、锁屏、转屏或返回主 Activity 均停止任务。
12. **首包仅为 Debug MVP。** 没有连接真机时，构建/单测/Lint/签名通过不等于真实游戏稳定；发布签名和商店分发不在本轮范围。

1. **不基于口袋乐游APK改包。** 原因：曲库为私有加密snapshot、维护性低、权限膨胀、targetSdk过旧。
2. **借鉴交互而非代码。** 借鉴悬浮控制、坐标采集和内置曲库入口；重新实现界面与逻辑。
3. **现有TXT是一等输入。** 161首无需人工重录；同时增加可版本化JSON中间格式。
4. **校准优先于写死比例。** 视频比例只用于预设，最终由用户把3×7网格对准实际白点。
5. **坐标按游戏区域归一化。** 不以整屏绝对像素存储，避免刘海、导航条和分辨率变化。
6. **无Root、离线、最小权限。** 不使用短信、电话、安装包、设备管理、全盘存储或查询全部应用权限。
7. **停止优先。** 暂停、失焦、转屏、锁屏和紧急停止必须取消未派发事件。
8. **先Android。** iOS跨应用悬浮和触摸派发受系统限制，不纳入首版承诺。
