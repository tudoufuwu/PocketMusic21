# 21键手机播放器 Android 版实施规划

规划时间：2026-08-09 22:00（UTC+8）

## 目标

重新制作一个干净、离线优先、可长期维护的 Android 21键播放器，首要适配《一梦江湖》横屏演奏界面，并直接兼容现有161首TXT曲谱。借鉴口袋乐游的悬浮控制与坐标采集思路，但不复制其加密代码、snapshot曲谱、账号或权限体系。

## 当前基线

- Windows新版播放器：`F:\codexai\01\music_player_next`
- 当前版本：`1.0.0-beta.22`
- 曲库：161首，曲库版本136
- TXT解析语义：按键或和弦 + beats；支持休止符
- 安卓参考APK静态分析：`F:\codexai\01\apk_assessment_pocketmusic`
- APK分析结论：功能路线可借鉴；私有Auto.js/Rhino snapshot与TXT不兼容；不作为代码底座
- 开发机：JDK 21和ADB已存在；Android SDK、sdkmanager、Gradle尚未安装

## 产品约束

1. 首发仅做Android；iOS不承诺跨应用悬浮和自动点击。
2. 不要求Root、Shizuku、账号或联网。
3. 仅申请悬浮窗、无障碍、必要前台服务等最小权限。
4. 曲谱新增不重新打包APK：支持TXT文件选择、批量导入和内部JSON。
5. 用户无需填写21组像素坐标：默认3×7网格，支持四边、三行、七列和单点微调。
6. 坐标归一化保存，并按游戏、分辨率、方向、安全区保存多套配置。
7. 失去游戏前台、切竖屏、锁屏或用户紧急停止时立即释放任务，禁止继续乱点。
8. 每个阶段必须留下源码、测试、APK或明确阻塞记录。

## 技术结构

```text
mobile_player_android/
├─ app/                         Android UI与服务
├─ core/                        TXT解析、事件模型、调度器
├─ library/                     161首内置曲库与元数据
├─ calibration/                 3×7网格、坐标配置
├─ automation/                  AccessibilityService手势派发
├─ tests/                       单元与设备测试
├─ artifacts/                   测试APK、哈希、截图
├─ PROGRESS.md                  独立续跑日志
├─ DECISIONS.md                 已锁定设计决策
└─ HANDOFF.md                   后续AI接手点
```

计划使用 Kotlin + Jetpack Compose；核心事件格式：

```json
{
  "schema": 1,
  "title": "示例",
  "beat_ms": 500,
  "events": [
    {"keys": ["a"], "beats": 1},
    {"keys": ["d", "g"], "beats": 0.5},
    {"rest": true, "beats": 1}
  ]
}
```

## 明日执行安排

### M0：开发环境（约1–2小时）

- 安装或配置Android SDK、platform-tools、build-tools与Gradle Wrapper。
- 创建最小Compose项目，锁定横屏测试Activity。
- 完成`assembleDebug`并保存空壳APK与SHA256。
- 若网络下载失败，记录缺失包和可复现命令，不破坏其他项目。

### M1：曲库与解析器（约2–3小时）

- 将现有TXT语法移植为Kotlin纯函数解析器。
- 导入161首内置曲谱及推荐节拍，不复制Windows运行代码。
- 添加全库解析测试、非法按键、和弦、休止、UTF-8 BOM测试。
- 完成曲库搜索、选择、播放速度设置的基本界面。

### M2：网格校准（约3–4小时）

- 实现3×7透明网格与21个圆心。
- 支持整体移动、宽高缩放、行列调整、单点微调。
- 实现16:9、18:9、19.5:9、20:9和16:10预设。
- 坐标使用游戏可用区域归一化值，处理刘海和导航栏安全区。
- 增加21点逐个闪烁/点击测试模式。

### M3：悬浮控制与自动演奏（约4–6小时）

- 添加`TYPE_ACCESSIBILITY_OVERLAY`或应用悬浮窗控制面板。
- 添加AccessibilityService及明确的用户授权引导。
- 实现单键、同时多键、休止、暂停、继续、停止。
- 悬浮球只保留播放/暂停/停止/收起；校准时才拦截触摸。
- 添加前台应用检查、横屏检查和紧急停止。

### M4：首包验收（约2小时）

- 单元测试、全库解析测试、静态权限检查。
- 生成第一份可安装测试APK及SHA256。
- ADB安装/启动验证；若无连接真机，则标记为“模拟/启动门禁通过，待用户真机校准”。
- 更新`PROGRESS.md`与`HANDOFF.md`。

## 时间承诺范围

- 第一天：目标产出可安装MVP APK；若Android SDK下载正常，预计工作量12–18小时。
- 第二天：结合真机截图/录屏完成一梦江湖校准、点击、和弦与后台行为修正。
- 第三至第五天：多分辨率、异常恢复、导入体验和发布质量完善。
- 不承诺绝对零Bug；以测试门禁、可回退APK和真机复现记录控制质量。

## Token预算

- 首个MVP：120k–200k
- 真机可用版本：250k–400k
- 充分测试版本：400k–600k
- 每个阶段独立落盘，额度不足时可由其他AI从`HANDOFF.md`继续。

## 验收门禁

- 161/161内置曲谱解析成功。
- 网格21点唯一、全部位于校准区域内。
- 暂停/停止后不再派发任何手势。
- 和弦同批派发，不退化成明显琶音；设备限制必须记录。
- APK可安装、可启动、无不必要的短信/电话/安装包/全盘存储权限。
- 源码、构建命令、测试结果、APK路径与SHA256全部写入日志。

## 后续需要的真机事实

这些信息不阻塞M0–M2，但进入M3/M4真机适配时需要：

- 手机型号与Android版本
- 一梦江湖横屏弹琴界面原始截图或约10秒录屏
- 系统是否使用手势导航、是否隐藏导航条
- 用户实际常用琴键布局是否固定为3×7白点
