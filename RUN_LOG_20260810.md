# Android implementation run log — 2026-08-10

## 2026-08-18 增量更新

- 小窗完整曲库：移除 8 首截断，列表扩展为可滚动 240dp。
- 校准批量操作：按钮“三行上下”“七列左右”，分别限制为 Y/X 方向。
- 播放抗干扰：移除横屏停止条件；目标应用失焦或锁屏仍安全停止。
- 录制/流通：`RecordingSession` 记录实际派发事件，支持导出当前/录制 TXT。
- 检查：`testDebugUnitTest assembleDebug` 返回 0；APK SHA-256 `8CB19912A419D974108C4B8AE1E3C7E386A46DE0789D1B987F6AA79F827797BB`。

All timestamps use Asia/Shanghai (UTC+8). This file is UTF-8.

## M0 — build environment and launchable shell (complete)

### Verified facts

- Host had JDK 21 and ADB 36.0.0, but no Android SDK, `sdkmanager`, Gradle, or Gradle wrapper.
- Downloaded Android command-line tools for Windows `15859902` from Google's official repository.
  - SHA-256: `90ae805d20434428bffcb699c290860f19bb5f66a67e6b330067e3de801fb04a`
- Installed a project-private SDK under `.android-sdk/`:
  - `platforms;android-35`
  - `build-tools;35.0.0`
  - `platform-tools` 37.0.1
- Downloaded Gradle 8.9 from the official Gradle distribution service.
  - SHA-256: `d725d707bfabd4dfdc958c624003b3c80accc03f7037b5122c4b1d0ef15cecab`
- Created a Kotlin/Jetpack Compose Android application with package `com.shadowtrace.pocketmusic21`, minSdk 26, targetSdk/compileSdk 35, and landscape activity.

### Decisive commands

```powershell
$env:ANDROID_HOME='F:\codexai\01\mobile_player_android\.android-sdk'
$env:ANDROID_SDK_ROOT=$env:ANDROID_HOME
.\.tools\gradle-8.9\bin\gradle.bat --no-daemon assembleDebug
```

- Return code: `0`
- Build result: `app/build/outputs/apk/debug/app-debug.apk`
- Preserved artifact: `F:\codexai\01\mobile_player_android\artifacts\PocketMusic21-M0-0.1.0-debug.apk`
- Size: `9,452,666` bytes
- SHA-256: `28A4D9E1CEADB47840E82A823D039236A4ED86BE8158053D3C64770CA3F4DEAC`
- `aapt2 dump badging` verified package/version/minSdk/targetSdk and launcher metadata.

### Failed path retained

- The first `gradlew.bat assembleDebug` attempt timed out after 10 seconds while the wrapper re-downloaded Gradle. It returned code `1`.
- `gradle-wrapper.properties` now sets `networkTimeout=120000`; the already verified local Gradle distribution provided the successful narrow path.

### Next

M1: copy the 161 TXT songs into Android assets, port the strict parser, extract recommended beat metadata, and prove 161/161 via JVM tests before expanding the UI.

## M1 — TXT library and selection UI (complete)

- Copied all 161 source TXT scores into deterministic Android asset names and generated `app/src/main/assets/library.json` with title, recommended beat, asset path, and source-content SHA-256.
- Ported the Windows parser rules: UTF-8 BOM, comments, 21 playable keys, `p`-only rest, unique chord keys, and finite beat range `(0, 64]`.
- Added library search, multi-document TXT import, beat adjustment, preview statistics, and a queue.
- JVM gate: `161/161` bundled TXT files parsed successfully.

## M2 — normalized 3×7 calibration (complete)

- Added 21 unique normalized points for `QWERTYU / ASDFGHJ / ZXCVBNM`.
- Added 16:9, 18:9, 19.5:9, 20:9, and 16:10 presets.
- Added point, row, column, and whole-grid drag scopes with bounds clamping and SharedPreferences persistence.
- Unit tests verify five presets, uniqueness, bounds, and edit-scope isolation.

## M3 — accessibility gestures and overlay (implementation complete; device validation pending)

- Added user-authorized AccessibilityService with `canPerformGestures=true` and `canRetrieveWindowContent=false`.
- Added `TYPE_ACCESSIBILITY_OVERLAY` controls for start/resume, pause, stop, and collapse; no ordinary overlay permission is requested.
- Added simultaneous chord strokes, rests, speed-aware timing, external-package focus checks, landscape/unlocked checks, cancellation, and Activity resume stop behavior.
- The user must load a song in the app and start it from the overlay while the game is foreground. The external package is captured from window-change events without reading window content.

## M4 — host acceptance and final debug APK (complete except real-device run)

### Final gate

```powershell
F:\codexai\01\mobile_player_android\build.ps1 -Clean
```

- Return code: `0`
- Gradle wrapper: 8.9; wrapper download retry was fixed with `networkTimeout=120000`.
- Unit tests: 6, failures 0, errors 0.
- Android lint: `No issues found.`
- APK contains exactly 161 `assets/songs/song_*.txt` entries.
- APK signature verification: v2 debug signature valid.
- `aapt2` verified package `com.shadowtrace.pocketmusic21`, version `0.1.0-mvp`, compile/target SDK 35, min SDK 26, and launcher activity.
- No SMS, phone, contacts, storage, network, package-install, device-admin, or `SYSTEM_ALERT_WINDOW` permission is requested.

### Final artifacts

- Canonical: `F:\codexai\01\mobile_player_android\artifacts\PocketMusic21-v0.1.0-mvp-debug.apk`
- Desktop copy: `C:\Users\rmb\Desktop\21键手机播放器-v0.1.0-MVP.apk`
- Size: `9,686,589` bytes
- SHA-256: `34BFB14BB633ED0E0C9C4832D767DE9DD7BF0DC31BA38237AFB9C9D3AA2B6422`

### External block

`adb devices -l` returned no connected device. Installation, activity launch, accessibility authorization, overlay touch behavior, exact game calibration, OEM background policy, and simultaneous multi-touch behavior remain unverified on hardware. This is not represented as a stable release.

## MuMu 12 emulator acceptance — 2026-08-10 (complete)

### Environment

- MuMu version: `5.30.1.3586`; manager: `D:\一梦江湖\MuMuPlayer-12.0\nx_main\MuMuManager.exe`.
- Instance 0: `MuMu安卓设备`, ADB `127.0.0.1:16384`, Android 12 / API 32, reported model Redmi 2201116SC.
- Launch command returned 0 and Android reached `start_finished` after 15 seconds.

### Runtime evidence

- `adb install -r` returned `Success`.
- Cold launch returned `Status: ok`, activity `com.shadowtrace.pocketmusic21/.MainActivity`, first measured total time 982 ms.
- Main screen rendered 161 songs and parsed the initial song on-device.
- Calibration screen rendered 21 points. Runtime screenshot exposed that the left preset/save controls overflowed at 1920×1080; the left column was changed to vertical scrolling, rebuilt, reinstalled, and verified with all five presets and Save reachable.
- AccessibilityService appeared in both Enabled and Bound service lists; Crashed services remained empty.
- Accessibility overlay rendered, moved by dragging its label, and provided start/resume, pause, stop, and collapse controls.
- A dedicated safe fixture module `mumuTapFixture` recorded real injected events at the normalized targets.
  - First single-note run recorded coordinates such as Q `(211,260)`, E `(710,260)`, J `(1709,519)` on the 1920×1038 usable area.
  - Chord fixture `ad 2` produced two `ACTION_DOWN` records at the same millisecond: pointer counts 1 and 2, proving simultaneous multi-stroke dispatch instead of arpeggiation.
  - Correct pause test: touch count stayed `4 → 4` across three paused seconds.
  - Correct stop test: touch count stayed unchanged across three seconds after stop.
- No application crash/ANR or accessibility-service crash was observed.
- Real game package `com.netease.wyclx` launched successfully with the overlay visible. The client then required a `3306.70 MB` resource download, so the actual 21-key performance screen is not yet reachable.
- The game screenshot exposed a stale overlay label after an external safety stop. The overlay was changed to poll controller state every 250 ms and to use a fixed-width, ellipsized status field; rebuild/reinstall verified the game overlay now changes to `已返回播放器，播放停止` automatically and its buttons no longer shift with title length.

### Evidence files

- `artifacts/mumu/main.png`
- `artifacts/mumu/calibration.png`
- `artifacts/mumu/calibration_scroll.png`
- `artifacts/mumu/overlay.png`
- `artifacts/mumu/overlay_drag.png`
- `artifacts/mumu/gesture_test.png`
- `artifacts/mumu/wyclx_launch.png`
- `artifacts/mumu/status_stop_refresh.png`
- `mumuTapFixture/` (safe local touch target and logging source)

### Permission cross-check

MuMu `dumpsys package` reports a virtual/granted `QUERY_ALL_PACKAGES`, but both the original APK and the byte-identical installed `base.apk` return no such request from `aapt2 dump permissions`; their SHA-256 values are identical. This is recorded as emulator/package-manager behavior, not an application manifest permission.

### Remaining boundary

MuMu proves the Android implementation and simultaneous gesture path. It does not replace a physical phone test for OEM background restrictions, touch injection policy, safe-area differences, or the actual game layout.


## M5 — 悬浮曲库、自动选歌与游戏内网格校准（2026-08-10 完成）

### 已实现

- 悬浮面板不再依赖主界面预先装歌：可直接搜索 161 首、点歌即播。
- 加入播完停止/顺序播放/单曲循环、最近播放、收藏、每曲速度、推荐速度和事件进度。
- 收起后为 52dp 可拖动音乐球；播放/暂停/错误用不同状态显示。
- 悬浮面板内加入“校准”：透明覆盖在游戏之上，可整体移动、横纵缩放 3×7 网格、单点精修并保存归一化坐标。
- 自动图像识别暂不作为主路径：需先取得真实琴键页截图验证主题、特效、缩放和遮挡下的稳定性；手动网格是确定性回退路径。

### 可复现验证

- `build.ps1` 返回 0；单元测试、lint、assemble 全部通过。
- MuMu 安装 `Success`；悬浮曲库截图：`artifacts/mumu/overlay_v2.png`。
- 游戏内透明校准层截图：`artifacts/mumu/calibration_overlay.png`；拖角后横纵缩放截图：`artifacts/mumu/calibration_scaled.png`。
- 安全触摸靶中点第一首后面板自动收起，3 秒内继续产生真实键位事件；截图：`artifacts/mumu/autoplay_v2.png`。
- 当前 APK：`artifacts/PocketMusic21-v0.1.0-mvp-debug.apk`，桌面副本字节一致。
- 大小：9,686,589 字节；SHA-256：`34BFB14BB633ED0E0C9C4832D767DE9DD7BF0DC31BA38237AFB9C9D3AA2B6422`。

### 下一步

等待《一梦江湖》资源下载完成并进入真实 21 键弹琴页；在该页打开“校准”，对齐并保存，然后做单音/和弦/暂停/停止验收。若真实截图的琴键视觉特征稳定，再增加自动识别辅助，不替代手动校准。
- 10:55 游戏下载实况：`202.19M / 2677.39M`，约 `6314.88KB/s`；截图 `artifacts/mumu/wyclx_current.png`。实例 0 保持下载，参考 APK 的实例 1 已关闭以节省资源。


## 手机真实琴键截图测量 — 2026-08-10

- 原始证据：`artifacts/phone/wyclx_1280x576_performance.jpg`
- 分辨率：1280×576（20:9）；SHA-256：`C8BF62DE2A5ED174201F6B2A3B90D6CA579951B9113BFE661AEBFE9CC23BF4FC`。
- 21 个点呈严格 3×7 规则阵列。测量中心：X=`310,437,564,691,818,945,1072`；Y=`363,436,508`。
- 已新增 `一梦江湖20:9（截图）` 校准预设；新安装默认使用该预设，旧安装可在主校准页或游戏内校准层点“截图预设”。
- 预设仍保留整体移动、横纵缩放和单点修正，避免截图裁切、系统安全区或游戏 UI 缩放造成少量偏差。
- 新测试验证首末点归一化坐标；`build.ps1` 返回 0，测试/lint/assemble 通过。
- 最新 APK 大小 9,686,589 字节；SHA-256：`34BFB14BB633ED0E0C9C4832D767DE9DD7BF0DC31BA38237AFB9C9D3AA2B6422`。


## 真机悬浮遮挡修复 — 2026-08-10

- 用户证据：`artifacts/phone/overlay_obstruction_1280x576.jpg`，SHA-256：`05E4A21F46DABB064867266AA72AAE437C75F74463D477E7DAB3C803DF6AF46A`。
- 事实：展开面板约占真机左侧 540×576；Accessibility Overlay 在自身矩形内必然消费触摸，因此其下方琴键无法响应。这不是琴键坐标错误。
- 修复：展开宽度由 360dp 降为不超过 300dp/屏宽38%，曲目区缩短；标题状态区明确显示“☰ 拖动”并扩大为拖动热区。
- 修复：点击播放或点歌即播后，无论启动成功还是显示错误，面板都立即收成 52dp 音乐球，避免继续遮住左侧琴键；音乐球仍可拖动和重新展开查看状态。
- uild.ps1 返回 0，测试/lint/assemble 通过。模拟器已由用户关闭，因此本次简单 UI 回归未再次启动 MuMu；等待真机覆盖安装验证。
- APK SHA-256：`34BFB14BB633ED0E0C9C4832D767DE9DD7BF0DC31BA38237AFB9C9D3AA2B6422`。
